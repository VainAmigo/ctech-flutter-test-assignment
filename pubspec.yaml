name: ctech_flutter_test_app
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.9.2
  connectivity_plus: ^7.1.1
  shared_preferences: ^2.5.5
  meta: ^1.17.0
  flutter_bloc: ^9.1.1
  json_serializable: ^6.14.0
  json_annotation: ^4.12.0
  build_runner: ^2.15.0
  url_launcher: ^6.3.2
  flutter_svg: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/icons/
